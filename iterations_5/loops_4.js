const marks ={
    maths : 30,
    english : 40,
    physics : 34,
    chemistry : 31,
    biology : 45,
}

for (const key in marks) {
    // console.log(`${key} marks : ${marks[key]}/50`);
}


const myarray = [21, 34, 23, 42, 12, 55]

for (const key in myarray) {
    // console.log(myarray[key]);
}

// const maps = new Map()
// maps.set('name', "fortin")
// maps.set('age', 21)
// maps.set('github', true)

// for (const key in maps) {
//     console.log(key)
// }................................not iterable